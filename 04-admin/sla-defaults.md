# SLA Defaults & Overrides

## Default SLA
Admin Console → SLA Settings → Set response & resolution times.

## Per-Client Override
Client Directory → SLA tab → Override as needed.

## Priority Tiers
Supports P1–P4 SLA levels.
